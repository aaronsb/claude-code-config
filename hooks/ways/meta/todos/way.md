---
trigger: context-threshold
threshold: 75
---
**Context checkpoint.** You have no active task list and context is filling up. Run `TaskList` — if empty, use `TaskCreate` now. Capture: current goal, progress so far, next steps, key decisions made. This repeats every prompt until you do.
